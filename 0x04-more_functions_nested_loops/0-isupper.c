#include "main.h"

/**
 * _isupper - return a valur for upper case
 * @c: defines c as some integer
 * Return: returns 1 for success
 */

int _isupper(int c)
{
	int i;

	if (i >= 'A' && i <= 'Z')
	{
		return (1);
	}
	return (0);
}
