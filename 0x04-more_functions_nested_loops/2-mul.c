#include "main.h"

/**
 * mul - return a valur for upper case
 * @a: defines c as some integer
 * @b: defince some
 * Return: returns 1 for success
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
