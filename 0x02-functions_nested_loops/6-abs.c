#include "main.h"

/**
 * _abs - prints alphabet 10 times
 *
 * Description: Make a while loop'
 * @n: ascii value to be tested
 * Return: void
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n - 2 * n);
	}
}
