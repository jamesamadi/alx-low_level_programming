#include "main.h"

/**
 * _islower - prints alphabet 10 times
 *
 * Description: Make a while loop'
 *
 * Return: void
 */

int _islower(void);
{
	int c;

	for (c >= 'a'; c <= 'z';)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
