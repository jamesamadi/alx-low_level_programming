#include "main.h"

/**
 * _islower - prints alphabet 10 times
 *
 * Description: Make a while loop'
 * @c: ascii value to be tested
 * Return: void
 */

int _islower(int c);
{
	for (c = 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
