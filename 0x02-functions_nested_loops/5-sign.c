#include "main.h"

/**
 * print_sign - prints alphabet 10 times
 *
 * Description: Make a while loop'
 * @n: ascii value to be tested
 * Return: void
 */

int print_sign(int n)
{
	if (n > 1)
	{
		_putchar(1)
		return (1);
	}
	else if (n < 1)
	{
		_putchar('-')
		return (-1);
	}
	else
	{
		_putchar('0')
		return(0)
	}
}
