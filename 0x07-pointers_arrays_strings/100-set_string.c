#include "main.h"

/**
 * set_string - returns pointer to the searched string
 * @s: string to be scanned
 * @to: string to scan for.
 * Return: the string of found words
 */

void set_string(char **s, char *to)
{
	*s = to;
}
